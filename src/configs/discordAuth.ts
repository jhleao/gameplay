export const { REDIRECT_URI } = process.env; 
export const { SCOPE } = process.env; 
export const { RESPONSE_TYPE } = process.env; 
export const { CLIENT_ID } = process.env;  
export const { CDN_IMAGE } = process.env; 
